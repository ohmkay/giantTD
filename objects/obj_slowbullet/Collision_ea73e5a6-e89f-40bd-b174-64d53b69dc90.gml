/// Destroy Object

var xpos = x;
var ypos = y;
/*
for(i = 0; i < o_wavecontrol.gore; i++) {
	instance_create_layer(x,y,"TurretLayer",obj_debri); }

*/
	with (other) {
		path_speed = 1;
		alarm[1] = room_speed * slowdelay;
		}

instance_destroy();

