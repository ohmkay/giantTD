/// Destroy Object

var xpos = x;
var ypos = y;



with (other) {
	//distance = point_distance(x,y,xpos,ypos)
	//if (distance < 40) {
	hp -= 1; }
	
	for(i = 0; i < o_wavecontrol.gore; i++) {
	instance_create_layer(x,y,"TurretLayer",obj_debri); }


